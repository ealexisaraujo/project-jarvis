#pragma once

// Copy this file to secrets.h and fill in local station credentials.
// secrets.h is ignored by Git and must never be committed.
#define PROJECT_JARVIS_WIFI_SSID ""
#define PROJECT_JARVIS_WIFI_PASSWORD ""
